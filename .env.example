PORT=3000
NODE_ENV=development
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/health_unveiled_dev"

# Phase 4 — admin auth
# JWT_SECRET=   # 64-byte random hex: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# Phase 4 — Cloudflare analytics
# CF_ANALYTICS_TOKEN=
# CF_ZONE_ID=
# CF_ACCOUNT_ID=
# CF_WEB_ANALYTICS_SITE_TAG=   # Requires Cloudflare Pro plan

# Optional — contact form notification
# NOTIFICATION_EMAIL_ENDPOINT=
